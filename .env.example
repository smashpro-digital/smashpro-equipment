# Public endpoint only. No provider keys or private HQ endpoint belong in VITE_*.
# Default uses a same-origin proxy; the HQ service requires a separately approved deployment.
VITE_SHIPMENT_ENDPOINT=/api/fleet/shipment/SP-ARDHI-26
